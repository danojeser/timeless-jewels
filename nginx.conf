server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location / {
        index index.html;

        # SPA fallback: rutas desconocidas sirven index.html
        try_files $uri $uri/ /index.html;
    }
}
